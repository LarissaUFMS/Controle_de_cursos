<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Controle_de_cursos"
        tools:targetApi="31">
        <activity
            android:name=".views.AlunoView"
            android:exported="false" />
        <activity
            android:name=".views.AlunoList"
            android:exported="false" />
        <activity
            android:name=".views.CursoView"
            android:exported="false"
            android:label="@string/title_activity_curso_view"
            android:theme="@style/Theme.Controle_de_cursos.NoActionBar" />
        <activity
            android:name=".views.CursoList"
            android:exported="false" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>